{
  "name": "beavshubv2",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "lint-api": "cd api && npm run lint",
    "lint-client": "cd client && npm run lint",
    "install-jest": "cd api && npm install",
    "jest": "cd api && npm run test",
    "install-playwright": "cd playwright && npm install -g yarn && yarn install --frozen-lockfile",
    "install-playwright-deps": "cd playwright && yarn playwright install --with-deps",
    "playwright": "cd playwright && npx playwright test",
    "prepare": "husky install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "husky": "^8.0.0"
  }
}
